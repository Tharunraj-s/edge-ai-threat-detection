# Webcam Threat Detection

This repo now contains only a minimal webcam detection script.

## Setup

```bash
pip install -r requirements.txt
```

## Run

```bash
python webcam_detect.py --weights best.pt --camera 0 --conf 0.5
```

Press `q` to quit the webcam window.
